import React from 'react';

export default function Header() {
    return (
        <header>
            <h2>
                Drag any image to rearrange the gallery
            </h2>
        </header>
    );
}
